{
    "builds": [
      {
        "src": "api/app.py",
        "use": "@vercel/python",
        "buildCommand": "sudo apt-get install pkg-config libcairo2-dev"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "api/app.py"
      }
    ]
  }